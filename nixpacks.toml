# Railway builds from repo root
[phases.setup]
nixPkgs = ["nodejs_20"]

[phases.install]
cmds = [
  "cd server && npm ci --include=dev"
]

[phases.build]
cmds = [
  "cd server && npm run build",
  "ls -la server/dist || echo 'dist folder not created'"
]

[start]
cmd = "node server/dist/server.js"
